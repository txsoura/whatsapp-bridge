{
  "name": "bridge-gatekeeper",
  "version": "1.0.0",
  "private": true,
  "description": "Provisioning gatekeeper in front of Evolution API — the only thing holding the global admin key. Each consuming project gets its own scoped key that can only create instances under its own name prefix.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "add-project": "ts-node src/add-project.ts"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.11",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.4"
  }
}
